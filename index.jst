import Vue from 'vue'
import Vuex from 'vuex'
import common from './common' //引入store(仓库)token(初始state对象)

// 如果在模块化构建系统中，请确保在开头调用了 Vue.use(Vuex)
Vue.use(Vuex)

const store = () => new Vuex.Store({

  modules: {
    common,
  },
})
export default store;
